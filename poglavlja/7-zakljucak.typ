= Закључак
<zakljucak>

У раду је имплементирано и евалуирано паралелно генерисање фракталног стабла
у програмским језицима Python и Rust. Стратегија паралелизације заснива се на
подели стабла на независна подстабла на дубини `split_depth`, која се потом
обрађују паралелно. У Python-у паралелизација је остварена вишепроцесним
извршавањем посредством библиотеке `multiprocessing`, а у Rust-у вишенитним
извршавањем посредством библиотеке _Rayon_.

При секвенцијалном извршавању Rust је приближно 39 пута бржи за симетрично
стабло и приближно 34 пута бржи за асиметрично. Та разлика одражава суштинску
разлику у извршним моделима: Python интерпретира бајткод у виртуелној машини,
проверава типове динамички у току извршавања и управља меморијом бројачима
референци, а Rust преводи целокупни изворни код у машински извршни облик пре
покретања, без потребе за _runtime_ окружењем.

Евалуација јаког скалирања показала је да Python постиже убрзање до 2.055
(симетрично) и 1.741 (асиметрично) при $N = 8$, при чему убрзање за симетрично
стабло при $N = 8$ пада испод убрзања при $N = 4$. Централни узрок је _overhead_
управљања процесима: на Windows платформи свако покретање скупа процеса
подразумева покретање $N$ нових Python интерпретера (`spawn` метода), а пренос
резултата одвија се `pickle` серијализацијом NumPy низова. Тај _overhead_ расте
са $N$ и при $N = 8$ надмашује корист паралелизма. Rust остварује монотоно
убрзање до 3.244 (симетрично) и 3.355 (асиметрично) при $N = 8$, јер нити деле
меморијски простор и немају трошак серијализације. Ограничавајући фактор Rust
имплементације је Hyper-Threading: при $N = 8$ свих осам нити расподељује се
на четири физичка процесорска језгра, па прелазак са $N = 4$ на $N = 8$ не
доноси двоструко убрзање.

Резултати слабог скалирања потврђују исти образац. Python скалирано убрзање
пада испод 1 при $N = 2$ — паралелна верзија са двоструко већим проблемом ради
спорије него секвенцијална верзија са половичним проблемом — јер _overhead_
при малом проблему надмашује корист паралелизма. Густафсонов модел тумачи ово
тако да је процењена паралелна фракција практично нула, иако је реч о последици
_overhead_-а, а не о суштинским серијским деловима алгоритма. Rust остварује
монотоно растуће скалирано убрзање до 3.168 (симетрично) и 3.878 (асиметрично)
при $N = 8$, ограничено само Hyper-Threading ефектом.

Асиметрично стабло показује различит утицај на два језика. У Python
имплементацији неједнаки задаци погоршавају убрзање при $N = 4$ услед статичке
расподеле у `pool.map` — убрзање готово стагнира у поређењу са $N = 2$.
У Rust имплементацији алгоритам крађе посла (енг. _work-stealing_) из библиотеке
_Rayon_ динамички расподељује неједнаке задатке и тиме остварује израженију
предност него у симетричном случају.

Примењени Амдалов и Густафсонов модел пружили су корисну теоретску основу за
квантификацију паралелног потенцијала, али почивају на претпоставци да је
паралелна фракција константна и независна од броја процесорских јединица. У
пракси, _overhead_ у Python имплементацији расте са $N$, а ниједан модел не
узима у обзир Hyper-Threading ни термално пригушивање лаптоп процесора при
дуготрајним оптерећењима. Последица је да модели систематски прецењују убрзање
при $N = 8$, нарочито за Python.

Могући правци даљег истраживања укључују:

- замену `pickle` серијализације дељеном меморијом посредством
  `multiprocessing.shared_memory`, чиме би се елиминисао _overhead_ преноса
  резултата у Python имплементацији;
- евалуацију на системима са `fork` методом покретања процеса (Linux), ради
  искључивања _overhead_-а покретања интерпретера и издвајања утицаја
  серијализације;
- евалуацију на платформи са вишим бројем физичких процесорских језгара без
  Hyper-Threading-а, ради провере Амдалових и Густафсонових прогноза при $N > 4$;
- анализу утицаја параметра `split_depth` на убрзање у функцији броја
  процесорских јединица за различите величине проблема.
