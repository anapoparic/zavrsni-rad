= Имплементација

У овом поглављу описана је секвенцијална и паралелна имплементација генерисања 
фракталног стабла у програмским језицима Python и Rust. Иако обе имплементације 
следе исту стратегију поделе задатака, ова два језика се значајно разликују у 
начину управљања меморијом и остваривања паралелног извршавања. Те разлике 
директно одређују избор механизма паралелизације у свакој имплементацији.

Глобална брава интерпретера (енг. _Global Interpreter Lock_ — GIL) је
механизам у _CPython_ имплементацији који у сваком тренутку
дозвољава извршавање Python кода само једној нити. GIL постоји јер _CPython_ 
управља меморијом путем бројања референци — за сваки објекат прати се број 
показивача на њега, а меморија се ослобађа када тај број падне на нулу. 
Без заштите, истовремена измена бројача референци довела би до трке за подацима. 
Пошто сваки процес поседује сопствени интерпретер и меморијски простор, GIL не представља 
ограничење на нивоу процеса — вишепроцесно извршавање тако постаје природан избор за постизање
паралелности @ajitsaria2018.

Модел власништва (енг. _ownership_) централни је механизам програмског језика Rust
којим се гарантује безбедност меморије и избегава трка за подацима у вишенитним 
програмима без потребе за сакупљачем смећа (енг. _garbage collector_). Правилима 
власништва и позајмљивања, која се проверавају у фази превођења (енг. _compile time_), 
грешке конкурентног програмирања постају грешке компилације, а не грешке током извршавања 
(енг. _runtime_) — чиме GIL као механизам заштите постаје непотребан. @klabnik2025. Библиотека 
_Rayon_ искоришћава ове гаранције за безбедну паралелизацију преко нити @rayon.

== Секвенцијална имплементација <sec:sekvencijalna>

Генерисање фракталног стабла засновано је на рекурзивној функцији која у
сваком позиву генерише једну грану и рекурзивно обрађује леву и десну
подграну. Свака грана одређена је координатама почетне тачке $(x_1, y_1)$
и крајње тачке $(x_2, y_2)$, уз информацију о дубини $d$ на којој је
генерисана. Улазни параметри функције су:

- $(x, y)$ — координате почетне тачке гране,
- $l$ — дужина гране,
- $alpha$ — угао који грана заклапа са хоризонталом.

Крајња тачка израчунава се тригонометријски:

$ x_2 = x + l dot cos(alpha), quad y_2 = y + l dot sin(alpha) $

Када дужина $l$ падне испод минимума $l_"min"$, рекурзија
се прекида и грана се не генерише. У супротном, након што се текућа грана 
сачува, функција се позива два пута — једном за леву подграну са углом 
$alpha + theta$ и једном за десну подграну са углом $alpha - theta$. 
У оба случаја дужина гране скалира се фактором $r$, а крајња тачка $(x_2, y_2)$ 
тренутне гране постаје почетна тачка наредног позива.

Симетрично стабло користи исте параметре $r$ и $theta$ за обе подгране,
а асиметрично стабло уводи независне параметре — $(r_l, theta_l)$ за леву
и $(r_d, theta_d)$ за десну подграну. Rust и Python имплементације деле ову
рекурзивну логику, а разликују се у начину представљања и складиштења
грана, као и у моделу паралелизације.

== Стратегија поделе задатака <sec:podela-zadataka>

Будући да свака грана зависи од крајње тачке свог родитеља, алгоритам из
секције @sec:sekvencijalna не може се директно паралелизовати. Решење лежи у подели стабла
на независна подстабла. Горњи нивои стабла генеришу се секвенцијалним 
обиласком из секције @sec:sekvencijalna, уз једну измену у услову заустављања — обилазак не стаје на
минималној дужини гране, већ на дубини `split_depth`. Сваки чвор на тој
дубини постаје корен независног подстабла и бележи се као засебан задатак
описан координатама, дужином, углом и дубином. Горње гране складиште се
одвојено и на крају спајају са паралелно генерисаним гранама.

Вредност `split_depth` одређује се формулом:

$ "split_depth" = ceil(log_2(N times 4)) $

где је $N$ број расположивих процесорских јединица. Избор фактора 4 заснован је на 
принципу прекомерне расподеле задатака (енг. _oversubscription_): генерише се знатно 
више задатака него расположивих јединица. Тиме се осигурава да јединице које заврше 
раније одмах преузму нове задатке, уместо да беспослено чекају.
За симетрично стабло сва подстабла на дубини $d$ имају тачно исту величину —
равнотежа је увек савршена и вредност `split_depth` утиче само на секвенцијални
_overhead_. За асиметрично стабло, пошто лева и десна грана расту различитим факторима
смањења ($r_l eq.not r_d$), подстабла на истој дубини нису исте величине —
принцип _oversubscription_ је у том случају важнији.

За $N = 8$ формула даје $d = ceil(log_2(32)) = 5$. Емпиријска анализа за $N = 8$ и 
асиметрично стабло, приказана на слици @fig:split-depth, потврђује да хеуристика 
даје добре резултате. При $d = 5$ Rust остварује убрзање 3,90×, dok емпиријски оптимум 
при $d = 8$ износи 4,27× — разлика од приближно 10%. У Python имплементацији убрзање је 
приближно константно за $d >= 3$, јер трошак покретања процеса и серијализације резултата
доминира над неравномерношћу задатака, па вредност `split_depth` нема практичног утицаја.

#figure(
  image("../slike/split-depth-asym.png", width: 85%),
  caption: [Убрзање у зависности од `split_depth` за асиметрично стабло при $N = 8$.
    Испрекидана линија означава хеуристичку вредност $d = 5$.],
  placement: none,
) <fig:split-depth>

Хеуристика $d = 5$ представља практичан избор из два разлога.
Прво, при $d = 8$ серијски се обрађује $2^8 - 1 = 255$ горњих грана пре почетка
паралелног извршавања — осам пута више него при $d = 5$, што уноси значајан
_overhead_ трошак у серијску фазу. Друго, измерена добит од 10% специфична је за
конкретну структуру стабла и хардверску конфигурацију и не може се очекивати
у општем случају. Поред тога, формула $ceil(log_2(N times 4))$ додатно осигурава да се
дубина поделе аутоматски прилагођава броју расположивих јединица — за $N = 2$ даје $d = 3$, 
за $N = 4$ даје $d = 4$ — чиме _overhead_ трошак серијске фазе остаје у разумним границама 
за све вредности $N$.

== Паралелна Python имплементација <sec:python-impl>

Python имплементација остварује паралелизацију на нивоу процеса —
библиотека `multiprocessing` покреће независне процесе и тако омогућава
стварно паралелно извршавање на вишеструким процесорским јединицама.
Комуникација главног процеса са радним процесима одвија се посредством
`pickle` серијализације — задаци се пре слања пакују у бајт-ток (енг. _byte stream_), а резултати
при враћању распакују @psf2024docs. Имплементација се одвија у четири фазе.

У првој фази утврђује се број радних процеса $N$ — типично једнак
броју расположивих процесорских јединица — и израчунава `split_depth` по
формули из секције @sec:podela-zadataka. Као полазна тачка рекурзије, генерише се дебло стабла 
засебно и уписује у листу горњих грана `upper_branches`.

У другој фази примењује се стратегија поделе описана у секцији @sec:podela-zadataka и
производе се две листе: `upper_branches` са секвенцијално генерисаним 
горњим гранама и `tasks` са параметрима независних подстабала спремних 
за паралелну обраду.

У трећој фази задаци се дистрибуирају позивом `pool.map` над скупом процеса
`Pool(processes=N)`. За сваки независан задатак позива се секвенцијална функција из секције @sec:sekvencijalna
са одговарајућим параметрима из листе `tasks`. Пре почетка рекурзије унапред
се одређује укупан број грана у подстаблу. За симетрично подстабло, рачуна се формулом
$2^(d_"max" + 1) - 1$ из секције @sec:velicina-problema — и алоцира се _NumPy_ низ тачно
потребне величине, чиме се избегава трошак реалоцирања при динамичком расту.
Сваки ред низа садржи $(x_1, y_1, x_2, y_2, d)$. Рекурзивна функција уписује 
сваку грану директно на следећу слободну позицију у алоцираном низу и враћа 
низ грана главном процесу.

За асиметрично подстабло ова формула не важи јер лева и десна грана имају
различите факторе смањења — лева страна достиже $l_"min"$ на дубини 26,
а десна на дубини 18. Број грана на некој путањи зависи искључиво од тога колико 
пута је путања скренула лево, а колико десно — не од редоследа тих скретања. 
На пример, путање лево-десно и десно-лево обе производе грану исте дужине 
$L_0 dot r_l^a dot r_d^b$. Функција `_count_asymmetric` искоришћава ту особину
тако што стање рекурзије представља паром $(a, b)$, где је $a$ број левих, а $b$
број десних скретања од корена. Пошто многе различите путање дају исто стање $(a, b)$:
на пример, лево-десно и десно-лево обе дају $(1, 1)$, `lru_cache` декоратор осигурава
да се свако стање израчуна само једном и затим кешира — чиме се избегава поновно израчунавање.

#figure(
  ```python
  @lru_cache(maxsize=None)
  def count(a, b):
      if starting_length * (left_ratio ** a) * (right_ratio ** b) < min_length:
          return 0
      return 1 + count(a + 1, b) + count(a, b + 1)
  ```,
  caption: [Мемоизована функција за бројање грана асиметричног подстабла],
  placement: none
) <lst:count-asymmetric>

Стабло са преко 8 милиона грана своди се на свега 305 јединствених стања
над којима функција са листинга @lst:count-asymmetric оперише. Обична рекурзија
без мемоизације захтевала би позив функције за сваку грану — 8 464 173 Python
позива уместо 305, што би бројање претворило у уско грло.

У четвртој фази, листа `upper_branches` претвара се у _NumPy_ низ, а позивом
`np.concatenate` горње гране и резултати свих паралелних процеса спајају се у
јединствен низ свих грана стабла.

== Паралелна Rust имплементација

Насупрот Python приступу, Rust паралелизација остварује се на нивоу нити,
посредством библиотеке _Rayon_. Rust имплементација организована је по
истом принципу као Python верзија из секције @sec:python-impl — припрема, подела, паралелно
извршавање и спајање резултата. _Rayon_ омогућава паралелизам података
тако што секвенцијалне итераторе замењује паралелним еквивалентима путем
`par_iter()`, при чему логика алгоритма остаје непромењена. Захваљујући алгоритму
крађе посла, _Rayon_ самостално и равномерно распоређује посао међу нитима, без
потребе за ручним управљањем @rayon — посебно корисно када задаци нису једнаке
величине, као у случају асиметричног стабла. Стратегија поделе из секције @sec:podela-zadataka рекурзивни
проблем своди на равну листу независних задатака, над којом се паралелни итератор
примењује директно.

У првој фази утврђује се број нити $N$ позивом `thread::available_parallelism()`
— еквивалентно `cpu_count()` из секције @sec:python-impl — израчунава `split_depth` по истој
формули из секције @sec:podela-zadataka и гради се скуп нити са $N$ нити. Дебло стабла уписује се у
вектор `upper_branches` као вредност типа `Branch` — Rust структуре са пољима
$(x_1, y_1, x_2, y_2, d)$, еквивалентне реду у _NumPy_ низу из секције @sec:python-impl.

У другој фази обилазе се секвенцијално горњи нивои стабла и за сваки чвор на дубини
`split_depth` бележе се улазни параметри потребни за генерисање одговарајућег
подстабла, чиме се формира вектор задатака `tasks`.

Трећа фаза обухвата паралелно извршавање задатака. Позивом

```rust
pool.install(|| tasks.par_iter().map(...))
```

_Rayon_ аутоматски распоређује задатке преко скупа нити — еквивалентно
`pool.map` из секције @sec:python-impl. За сваки задатак позива се секвенцијална функција 
из секције @sec:sekvencijalna са одговарајућим параметрима дефинисаним у вектору `tasks`. Пре рекурзије, 
одређује се укупан број грана у подстаблу и алоцира се вектор типа `Vec<Branch>` тачне 
величине позивом `Vec::with_capacity`.

За симетрично подстабло бројање обавља функција `count_branches` рекурзијом

$ "count"(l) = 1 + 2 dot "count"(l dot r) $

Пошто обе подгране деле исти
фактор смањења $r$, лево и десно подстабло увек имају исти број грана — па је
довољно рекурзивно избројати само једно и помножити са 2. Функција тако прави
само $d_"max" + 1$ позива дуж једног ланца и даје исти резултат као формула
$2^(d_"max" + 1) - 1$ из секције @sec:velicina-problema, без потребе да обилази цело стабло.
Директна примена те формуле такође би дала тачан резултат, али рекурзивни приступ
је конзистентан са функцијом `count_branches_asymmetric` која следи — за
асиметрично стабло аналитичка формула не постоји.

За асиметрична подстабла бројање обавља функција `count_branches_asymmetric`
рекурзијом

$ "count"(l) = 1 + "count"(l dot r_l) + "count"(l dot r_d) $

Пошто
$r_l eq.not r_d$, лево и десно подстабло имају различит број грана — рекурзија
мора да обиђе оба смера и укупан број позива пропорционалан је броју грана у
подстаблу, као што је приказано на листингу @lst:count-asymmetric-rust.

#figure(
  ```rust
  pub fn count_branches_asymmetric(
      length: f64, left_ratio: f64, right_ratio: f64, min_length: f64,
  ) -> usize {
      if length < min_length { return 0; }
      1 + count_branches_asymmetric(length * left_ratio,  left_ratio, right_ratio, min_length)
        + count_branches_asymmetric(length * right_ratio, left_ratio, right_ratio, min_length)
  }
  ```,
  caption: [Рекурзивна функција за бројање грана асиметричног подстабла — Rust],
  placement: none
) <lst:count-asymmetric-rust>

Еквивалент мемоизације у програмском језику Rust захтевао би `HashMap` и прелазак 
на $(a, b)$ параметризацију, што уводи додатну сложеност без реалне користи — у 
компајлираном коду N рекурзивних позива траје реда величине микросекунди. У програмском 
језику Python сваки интерпретерски позив носи значајан _overhead_ трошак, па мемоизација 
са свега неколико стотина јединствених стања представља знатно ефикаснији приступ.

У завршној фази резултати сваке нити сакупљају се у структуру типа
`Vec<Vec<Branch>>`. Будући да је за сваку нит резервисан засебан вектор, спајање
резултата у јединствен низ није неопходно — укупан број грана добија се сабирањем
дужина појединачних вектора.

