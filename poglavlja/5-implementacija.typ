= Имплементација

У овом поглављу описана је секвенцијална и паралелна имплементација
генерисања фракталног стабла у Python-у и Rust-у. Иако Python и Rust верзија 
следе исту стратегију поделе задатака, два језика се суштински разликују у начину на који управљају
меморијом и остварују паралелно извршавање — ове разлике директно одређују
избор механизма паралелизације у свакој имплементацији.

Глобална брава интерпретатора (енг. _Global Interpreter Lock_ — GIL) је
механизам у _CPython_ имплементацији Python-а који у сваком тренутку
дозвољава извршавање Python кода само једној нити. GIL постоји јер је
управљање меморијом у Python-у засновано на бројању референци — за сваки
објекат прати се број показивача на њега, а меморија се ослобађа када тај
број падне на нулу. Без заштите, истовремена измена бројача референци довела
би до трке у приступу подацима. Пошто сваки процес поседује сопствени
интерпретатор и меморијски простор, GIL не представља ограничење на нивоу
процеса — вишепроцесно извршавање тако постаје природни избор за постизање
паралелности у Python-у @ajitsaria2018.

У Rust-у, модел власништва (енг. _ownership_) представља централни механизам
за обезбеђивање безбедности меморије и вишенитних програма без потребе за
сакупљачем смећа (енг. _garbage collector_). Правилима власништва и позајмљивања, која се проверавају у
фази превођења, многе грешке конкурентног програмирања постају грешке
компилације, а не грешке током извршавања — чиме се елиминише потреба за
механизмима попут GIL-а @klabnik2025. Библиотека _Rayon_ искоришћава ове
гаранције за безбедну паралелизацију преко нити @rayon.

== Секвенцијална имплементација

Генерисање фракталног стабла засновано је на рекурзивној функцији која у
сваком позиву генерише једну грану и рекурзивно обрађује леву и десну
подграну. Свака грана одређена је координатама почетне тачке $(x_1, y_1)$
и крајње тачке $(x_2, y_2)$, уз метаподатак о дубини $d$ на којој је
генерисана. Улазни параметри функције су:

- $(x, y)$ — координате почетне тачке гране,
- $l$ — дужина гране,
- $alpha$ — угао који грана заклапа са хоризонталом.

Крајња тачка израчунава се тригонометријски:

$ x_2 = x + l dot cos(alpha), quad y_2 = y + l dot sin(alpha) $

Када дужина $l$ падне испод унапред задатог минимума $l_"min"$, рекурзија
се прекида и грана се не генерише. У свим осталим случајевима, након што
се текућа грана забележи, функција се позива два пута — једном за леву
подграну са углом $alpha + theta$, и једном за десну са углом
$alpha - theta$, при чему се дужина у оба случаја скалира фактором $r$,
а крајња тачка $(x_2, y_2)$ тренутне гране постаје почетна тачка
наредног позива.

Симетрично стабло користи исте параметре $r$ и $theta$ за обе подгране,
а асиметрично стабло уводи независне параметре — $(r_l, theta_l)$ за леву
и $(r_d, theta_d)$ за десну. Rust и Python имплементације деле ову
рекурзивну логику, а разликују се у начину представљања и складиштења
грана и у моделу паралелизације.

== Стратегија поделе задатака

Будући да свака грана зависи од крајње тачке свог родитеља, алгоритам из
секције 4.1 не може се директно паралелизовати. Решење лежи у подели стабла
на независна подстабла — стабло се пресеца на дубини задатој параметром
`split_depth`, након чега се сваки део може обрађивати независно.

Горњи нивои стабла генеришу се истим рекурзивним обиласком описаним у
секцији 4.1, уз једну измену у услову заустављања — обилазак не стаје на
минималној дужини гране, већ на дубини `split_depth`. Сваки чвор на тој
дубини постаје корен независног подстабла и бележи се као засебан задатак
описан координатама, дужином, углом и дубином. Горње гране складиште се
одвојено и на крају спајају са паралелно генерисаним гранама.

Вредност `split_depth` одређује се формулом:

$ "split_depth" = floor(log_2(N times 4)) $

где је $N$ број расположивих процесорских јединица. Фактор 4 осигурава да
се генерише четири пута више задатака него расположивих процесорских јединица —
ако би свакој јединици припао тачно један задатак, јединице које заврше пре
других би беспослено чекале на преостале. Вишак задатака омогућава да свака
слободна јединица одмах преузме следећи задатак, чиме се искоришћеност
повећава нарочито у асиметричном стаблу где подстабла нису исте величине. 
Вредност фактора 4 одређена је на основу емпиријске анализе — при датој 
хардверској конфигурацији убрзање расте до одређене дубине поделе, а затим опада. 
За $N = 8$, `split_depth` износи 5 — на тој дубини стабло има $2^5 = 32$ чворова, 
односно 32 независна задатка за паралелно извршавање.

== Паралелна Python имплементација

Python имплементација остварује паралелизацију на нивоу процеса —
библиотека `multiprocessing` покреће независне процесе и тако омогућава
стварно паралелно извршавање на вишеструким процесорским јединицама.
Комуникација главног процеса са радним процесима одвија се посредством
`pickle` серијализације — задаци се пре слања пакују у бајт-ток, а резултати
при враћању распакују @psf2024docs. Имплементација се одвија у четири фазе.

У првој фази утврђује се број радних процеса $N$ — типично једнак
броју расположивих процесорских јединица — и израчунава `split_depth` по
формули из секције 4.2. Као полазна тачка рекурзије, дебло стабла генерише
се засебно и уписује у листу горњих грана `upper_branches`.

У другој фази примењује се стратегија поделе описана у секцији 4.2 и
производе се две листе: `upper_branches` са горњим гранама и `tasks` са
параметрима независних подстабала спремних за паралелну обраду.

У трећој фази задаци се дистрибуирају позивом `pool.map` над скупом процеса
`Pool(processes=N)`. За сваки задатак позива се секвенцијална функција из
секције 4.1 са одговарајућим параметрима. Пре почетка рекурзије, израчунава
се укупан број грана у подстаблу и алоцира се _NumPy_ низ тачно потребне
величине, при чему сваки ред садржи $(x_1, y_1, x_2, y_2, d)$.
За симетрично подстабло број грана одређује се формулом
$2^(d_"max" + 1) - 1$ из секције 2.4.

За асиметрично подстабло ова формула не важи јер лева и десна грана имају
различите факторе смањења — лева страна достиже $l_"min"$ на дубини 22,
а десна на дубини 17. Свака путања кроз стабло генерише различит број грана
у зависности од тога колико пута је скренула лево, а колико десно. Функција
`_count_asymmetric` искоришћава ту особину: уместо да броји гране за сваку
путању појединачно, она стање рекурзије представља паром $(a, b)$, где је
$a$ број левих, а $b$ број десних скретања од корена. Дужина гране у стању
$(a, b)$ износи $L_0 dot r_l^a dot r_d^b$, тако да је услов заустављања
одређен искључиво тим паром — не целом путањом. Многе различите путање кроз
стабло завршавају у истом стању $(a, b)$: на пример, лево-десно и десно-лево
обе дају $(1, 1)$. `lru_cache` декоратор осигурава да се вредност за свако
стање израчуна само једном и затим кешира.

#figure(
  ```python
  @lru_cache(maxsize=None)
  def count(a, b):
      if starting_length * (left_ratio ** a) * (right_ratio ** b) < min_length:
          return 0
      return 1 + count(a + 1, b) + count(a, b + 1)
  ```,
  caption: [Мемоизована функција за бројање грана асиметричног подстабла],
  placement: none
) <lst:count-asymmetric>

Стабло са преко 8 милиона грана своди се на свега око 459 јединствених стања над којима функција са листинга @lst:count-asymmetric оперише.
Рекурзивна функција уписује сваку грану директно на следећу слободну позицију
у алоцираном низу и враћа низ грана главном процесу.

У четвртој фази, листа `upper_branches` претвара се у _NumPy_ низ, а позивом
`np.concatenate` горње гране и резултати свих паралелних процеса спајају се у
јединствен низ свих грана стабла.

== Паралелна Rust имплементација

За разлику од Python-а, Rust паралелизација остварује се на нивоу нити,
посредством библиотеке _Rayon_. Rust имплементација организована је по
истом принципу као Python верзија из секције 4.3 — припрема, подела, паралелно
извршавање и спајање резултата. _Rayon_ омогућава паралелизам података
тако што секвенцијалне итераторе замењује паралелним еквивалентима путем
`par_iter()`, при чему логика алгоритма остаје непромењена. Захваљујући алгоритму
крађе посла, Rayon самостално и равномерно расподељује посао међу нитима, без
потребе за ручним управљањем @rayon. Стратегија поделе из секције 4.2 рекурзивни
проблем своди на равну листу независних задатака, над којом се паралелни итератор
примењује директно.

У првој фази утврђује се број нити $N$, израчунава `split_depth` по формули
из секције 4.2, и гради се скуп нити са $N$ нити. Дебло стабла уписује се у
вектор `upper_branches` као вредност типа `Branch` — Rust структуре са пољима
$(x_1, y_1, x_2, y_2, d)$, еквивалентне реду у _NumPy_ низу из секције 4.3.

У другој фази обилазе се горњи нивои стабла и за сваки чвор на дубини
`split_depth` бележе се улазни параметри потребни за генерисање одговарајућег
подстабла, чиме се формира вектор задатака `tasks`.

Трећа фаза обухвата паралелно извршавање задатака преко скупа нити. За свако
подстабло позива се секвенцијална функција из секције 4.1 са одговарајућим параметрима.
Пре рекурзије, одређује се укупан број грана у подстаблу и алоцира се вектор типа
`Vec<Branch>` тачне величине позивом `Vec::with_capacity`. За симетрична подстабла
бројање се врши функцијом `count_branches`, а за асиметрична функцијом
`count_branches_asymmetric`, при чему обе примењују рекурзију без мемоизације.
Када је исти фактор смањења примењен на обе гране стабла, сви задаци на дубини
`split_depth` садрже исти број грана и оптерећење је равномерно распоређено —
случај симетричног стабла. У асиметричном стаблу генеришу се задаци неједнаких
величина, јер лева и десна подстабла расту различитом брзином и достижу $l_"min"$
на различитим дубинама. Алгоритам крађе посла, поменут у уводу секције, управо је
намењен таквим сценаријима — неуједначена оптерећења расподељују се динамички.

У завршној фази резултати сваке нити сакупљају се у структуру типа
`Vec<Vec<Branch>>`. Будући да је за сваку нит резервисан засебан вектор, спајање
резултата у јединствен низ није неопходно — укупан број грана добија се сабирањем
дужина појединачних вектора.

== Верификација исправности

Паралелна расподела задатака не мења рачунску логику унутар сваког
подстабла — свако подстабло генерише исте гране као секвенцијална
функција примењена на исте улазне параметре. Ипак, спајање резултата
независних подстабала може унети грешке: неправилна индексација приликом
уписа у дељени низ, губитак задатка или погрешно спајање делимичних
резултата дали би нетачан скуп грана без видљиве грешке током извршавања.

Исправност паралелне имплементације верификована је поређењем излаза
паралелне верзије са излазом секвенцијалне верзије за исте улазне
параметре. Пошто редослед грана у паралелном излазу зависи од расподеле
задатака међу процесима, оба скупа грана сортирана су лексикографски по
координатама $(x_1, y_1, x_2, y_2)$ пре поређења. За верификацију су
коришћени параметри симетричног и асиметричног стабла из поглавља 6.

У Python имплементацији поређење је извршено _NumPy_ функцијом
`np.allclose` над сортираним низовима грана, са толеранцијом $10^{-10}$
ради искључивања могућих разлика у заокруживању. У Rust имплементацији
сви вектори подстабала спојени су у јединствен сортиран вектор и упоређени
са еквивалентним секвенцијалним излазом. У оба случаја потврђено је да
паралелни излаз садржи исти скуп грана као секвенцијална референца.

